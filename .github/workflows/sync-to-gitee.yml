name: 🚀 Sync GitHub Release to Gitee

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  sync-release:
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout 仓库代码（含 tag）
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: 🔖 获取最新 tag（actions-ecosystem）
      id: get-tag
      uses: actions-ecosystem/action-get-latest-tag@v1

    - name: 📌 写入 tag 到环境变量
      run: echo "LATEST_TAG=${{ steps.get-tag.outputs.tag }}" >> $GITHUB_ENV

    - name: 🧾 打印最新 tag
      run: echo "Latest GitHub tag is $LATEST_TAG"

    - name: 📦 下载 GitHub Release 附件
      run: |
        AUTH_HEADER="Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}"
        GH_API="https://api.github.com/repos/${{ github.repository }}/releases/tags/$LATEST_TAG"

        echo "📡 请求地址: $GH_API"
        curl -sSL -H "$AUTH_HEADER" "$GH_API" > release.json

        if ! jq -e '.assets' release.json > /dev/null; then
          echo "❌ 无法获取 Release 附件信息："
          cat release.json
          exit 1
        fi

        jq -r '.assets[] | "\(.name) \(.url)"' release.json > assets.txt

        while read -r name url; do
          echo "⬇️ 下载 $name ..."
          curl -L -H "$AUTH_HEADER" -H "Accept: application/octet-stream" "$url" -o "$name"
        done < assets.txt

    - name: 📤 上传到 Gitee Release
      run: |
        NAME="Release $LATEST_TAG"
        BODY="同步自 GitHub 的 Release $LATEST_TAG"
        GITEE_API="https://gitee.com/api/v5/repos/${{ secrets.GITEE_REPO }}/releases"

        echo "🚀 创建 Gitee Release..."
        response=$(curl -s -X POST "$GITEE_API" \
          -H "Content-Type: application/json" \
          -d "{
            \"access_token\": \"${{ secrets.GITEE_TOKEN }}\",
            \"tag_name\": \"$LATEST_TAG\",
            \"name\": \"$NAME\",
            \"body\": \"$BODY\",
            \"target_commitish\": \"master\"
          }")

        echo "$response"

        release_id=$(echo "$response" | grep -o '"id":[0-9]*' | grep -o '[0-9]*')

        if [ -z "$release_id" ]; then
          echo "❌ Gitee Release 创建失败。"
          exit 1
        fi

        echo "📤 开始上传附件..."
        for file in *; do
          [ -f "$file" ] || continue
          echo "📤 正在上传 $file 到 Gitee..."

          ENCODED_FILENAME=$(python3 -c "import urllib.parse; print(urllib.parse.quote('''$file'''))")
          RAW_REPO="${{ secrets.GITEE_REPO }}"
          GITEE_REPO_CLEAN=$(echo -n "$RAW_REPO" | tr -d '\n\r')
          UPLOAD_URL="https://gitee.com/api/v5/repos/$GITEE_REPO_CLEAN/releases/$release_id/assets"

          echo "🔍 原始 GITEE_REPO: [$RAW_REPO]"
          echo "🔍 清洗后路径: [$GITEE_REPO_CLEAN]"
          echo "🔗 上传地址: $UPLOAD_URL"
          echo "🔖 文件名: $file (urlencoded: $ENCODED_FILENAME)"

          http_code=$(curl -w "%{http_code}" -o /tmp/response.json -X POST "$UPLOAD_URL" \
            -H "Content-Type: multipart/form-data" \
            -F "access_token=${{ secrets.GITEE_TOKEN }}" \
            -F "name=$ENCODED_FILENAME" \
            -F "attachment=@$file")

          echo "🔎 返回状态码: $http_code"

          if [ "$http_code" != "201" ]; then
            echo "❌ 上传 $file 失败，响应如下："
            cat /tmp/response.json
            exit 1
          else
            echo "✅ 上传成功：$file"
          fi
        done
